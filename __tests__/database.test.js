describe('Database Connection', () => {
  it('should connect to the database', async () => {
    // This test will pass if the database connection is established
    // The connection is handled in setup.js
    expect(true).toBe(true);
  });
}); 